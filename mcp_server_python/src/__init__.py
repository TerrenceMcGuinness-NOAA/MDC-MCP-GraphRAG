"""MDC MCP/RAG Server — Python port (FastMCP + Strands Agents).

This package is a parallel, in-progress port of the Node.js MCP server in
``mcp_server_node/``. The Node.js server continues to serve production
traffic; this port is validated module-by-module via parity tests before
cutover (see ``.kiro/specs/python-mcp-server-port/``).
"""

__version__ = "1.0.0"
