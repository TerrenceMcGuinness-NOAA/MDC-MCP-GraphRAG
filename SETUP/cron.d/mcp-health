# DEPRECATED (Phase 63c, 2026-07-03): This cron file resurrected the retired
# eib-mcp-rag-static container every 5 minutes by calling `systemctl restart
# mcp-rag`. Kept in-tree only as Phase 63b rollback material — do NOT install
# to /etc/cron.d/ on new hosts. See .kiro/specs/retire-static-node-container/.

# MCP Gateway Health Monitoring
# Phase 23: Static Mode Multi-User Gateway
# Runs every 5 minutes

SHELL=/bin/bash
PATH=/usr/local/sbin:/usr/local/bin:/sbin:/bin:/usr/sbin:/usr/bin

*/5 * * * * root /opt/mcp/bin/health-check.sh 2>&1 | head -20

# Daily log rotation
0 0 * * * root find /var/log/mcp-health.log -size +10M -exec truncate -s 0 {} \;
